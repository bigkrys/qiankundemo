{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "shared/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
